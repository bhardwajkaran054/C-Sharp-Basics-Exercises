1- Write a program to count how many numbers between 1 and 100 are divisible by 3 with no remainder. Display the count on the console.

>>>
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace excercise
{
    class Program
    {
        static void Main(string[] args)
        {
            var count = 0;
            for (var i = 1; i < 100; i++)
            {
                if (i % 3 == 0)
                {
                    count += 1;
                    Console.WriteLine("{0} divisible by 3",i);
                }
            }
            Console.WriteLine("{0} numbers between 1 and 100 are divisible by 3 with no remainder.", count);
            
            
        }
    }
}
